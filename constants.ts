import { BlurType } from './types';

export const MIN_INTENSITY = 1;
export const MAX_INTENSITY = 30;
export const DEFAULT_INTENSITY = 5;
export const DEFAULT_BLUR_TYPE = BlurType.GAUSSIAN;

export const THEME_GRADIENT = "bg-slate-50";
export const CARD_BG = "bg-white border border-slate-200 shadow-sm";